name: tj_restock_mvp
description: MVP scanning restock application
publish_to: 'none'

# The following defines the version and build number for your application.
version: 0.1.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobile_scanner: ^5.0.0
  flutter_riverpod: ^2.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  csv: ^5.0.0
  share_plus: ^7.2.1
  path_provider: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.11
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true